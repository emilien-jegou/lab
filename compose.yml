services:
  db:
    image: pgvector/pgvector:pg15
    container_name: db
    env_file:
      - env/db.env
      - env/db.lab.env
    healthcheck:
      interval: 30s
      retries: 5
      start_period: 20s
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      timeout: 5s
    restart: unless-stopped
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./conf/postgres/initdb.d:/docker-entrypoint-initdb.d
    depends_on:
      - watchtower
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 128M

  lab:
    image: oven/bun:latest
    container_name: lab
    working_dir: /app
    env_file:
      - env/lab.env
      - env/db.lab.env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "6605:3000"
    expose:
      - 3000
    volumes:
      - ./lab/:/app
    command: bash -c "bun run dev"
    environment:
      - NODE_ENV=development
      - FORCE_COLOR=1
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M

  nocodb:
    image: "nocodb/nocodb:latest"
    container_name: nocodb
    env_file:
      - env/db.env
      - env/minio.env
    environment:
      NC_STORAGE_ADAPTER: s3
      NC_S3_ENDPOINT: http://minio:9000
      NC_S3_BUCKET: nocodb
      NC_S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER}
      NC_S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}
      NC_S3_FORCE_PATH_STYLE: "true"
    ports:
      - "6601:8080"
    volumes:
      - "nocodb_data:/usr/app/data"
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 256M

  minio:
    image: minio/minio:latest
    container_name: minio
    env_file:
      - env/minio.env
    ports:
      - "9000:9000"
      - "6602:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 128M

  create-minio-bucket:
    image: minio/mc
    depends_on:
      - minio
    env_file:
      - env/minio.env
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD;
      /usr/bin/mc mb myminio/nocodb --ignore-existing;
      "
    restart: "no"
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.5'

  valkey:
    image: valkey/valkey-bundle:latest
    container_name: valkey
    env_file: env/valkey.env
    healthcheck:
      test: ["CMD-SHELL", "valkey-cli -a $$VALKEY_PASSWORD ping | grep PONG"]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 20s
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - valkey:/data
    command: ["sh", "-c", "valkey-server --requirepass $$VALKEY_PASSWORD"]
    depends_on:
      - watchtower
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 32M

  searxng:
    image: searxng/searxng:latest
    container_name: searxng
    env_file:
      - env/searxng.env
      - env/valkey.env
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
      - DAC_OVERRIDE
    cap_drop:
      - ALL
    depends_on:
      valkey:
        condition: service_healthy
      watchtower:
        condition: service_started
    healthcheck:
      interval: 30s
      retries: 5
      start_period: 10s
      test: curl --fail http://localhost:8888/ || exit 1
      timeout: 3s
    ports:
      - "6604:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "1"
    restart: unless-stopped
    entrypoint: /usr/local/bin/entrypoint.sh
    volumes:
      - ./conf/searxng/settings.template.yml:/etc/searxng/settings.template.yml:ro
      - ./conf/searxng/entrypoint.sh:/usr/local/bin/entrypoint.sh:ro
      - ./conf/searxng/uwsgi.ini:/etc/searxng/uwsgi.ini:ro
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
        reservations:
          memory: 128M

  tika:
    image: apache/tika:latest-full
    container_name: tika
    env_file: env/tika.env
    healthcheck:
      interval: 30s
      retries: 5
      start_period: 5s
      test: curl --fail http://localhost:9998/tika || exit 1
      timeout: 5s
    ports:
      - "9998:9998"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    env_file: env/watchtower.env
    command: --cleanup --debug --interval 300
    restart: unless-stopped
    volumes:
      - /run/user/1000/podman/podman.sock:/var/run/docker.sock
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.2'
        reservations:
          memory: 32M

  crawl4ai:
    image: unclecode/crawl4ai:latest
    container_name: crawl4ai
    ports:
      - "6603:11235"
    shm_size: "1g"
    env_file: env/crawl4ai.env
    volumes:
      - /dev/shm:/dev/shm
    deploy:
      resources:
        limits:
          memory: 4G
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11235/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    user: "appuser"

volumes:
  nocodb_data: null
  minio_data: null
  db_data: null
  valkey: null
